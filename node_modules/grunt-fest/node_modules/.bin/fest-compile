#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../fest/bin/fest-compile" "$@"
  ret=$?
else 
  node  "$basedir/../fest/bin/fest-compile" "$@"
  ret=$?
fi
exit $ret
